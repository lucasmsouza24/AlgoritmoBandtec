// SSJ
function ssj() {
    var ki_base = Number(input_ki_base.value);
    var ki = ki_base * 50;
    display.innerHTML = `Após se transformar em SSJ, o KI desse Sayajin passa a ser de ${ki}.`;
}

// SSJ2
function ssj2() {
    var ki_base = Number(input_ki_base.value);
    var ki = ki_base * 100;
    display.innerHTML = `Após se transformar em SSJ2, o KI desse Sayajin passa a ser de ${ki}.`
}

// SSJ3
function ssj3() {
    var ki_base = Number(input_ki_base.value);
    var ki = ki_base * 200;
    display.innerHTML = `Após se transformar em SSJ3, o KI desse Sayajin passa a ser de ${ki}.`;
}